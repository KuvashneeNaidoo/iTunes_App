{
  "name": "itunes_app",
  "version": "1.0.0",
  "description": "iTunes App",
  "scripts": {
    "start": "npm install && concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && nodemon server.js",
    "frontend": "cd frontend && npm install && npm start"
  },
  "dependencies": {
    "concurrently": "7.3.0",
    "nodemon": "2.0.19"
  }
}
